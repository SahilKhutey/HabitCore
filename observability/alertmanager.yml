route:
  receiver: "healing-service"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: "healing-service"
    webhook_configs:
      - url: "http://healing-service:8080/heal"
